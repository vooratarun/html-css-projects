# LinkedIn Clone (Angular)

This project is now an Angular application converted from static HTML/CSS.

## Requirements

- Node.js 18+
- npm 9+

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:4200/`.

## Build

```bash
npm run build
```

Build output is generated in `dist/linkedin-clone`.

