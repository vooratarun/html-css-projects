export type SidebarItem = {
  icon: string;
  label: string;
  active?: boolean;
};

export type SectionItem = {
  icon: string;
  label: string;
  selected?: boolean;
};

export type EmailRow = {
  sender: string;
  subject: string;
  description: string;
  time: string;
};

