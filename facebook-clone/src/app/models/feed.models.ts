export interface SidebarItem {
  icon: string;
  label: string;
}

export interface Story {
  backgroundImage: string;
  avatar: string;
  title: string;
}

export interface Post {
  author: string;
  timestamp: string;
  message: string;
  avatar: string;
  imageUrl?: string;
}

